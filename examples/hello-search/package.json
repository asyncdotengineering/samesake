{
  "name": "samesake-example-hello-search",
  "type": "module",
  "private": true,
  "description": "Minimal search example: collection → push documents → index → hybrid search. No LLM required.",
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "@samesake/core": "workspace:*",
    "@samesake/server": "workspace:*"
  }
}
