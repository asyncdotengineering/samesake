# WBS — Search Quality & Latency (post-parity improvement wave)

2026-06-11. Baseline: samesake mean@10 2.338 / P@5 0.804 / priceViol 0.16 / 1,065ms (PARITY.md). Headroom map from per-type evals: local 1.50, style 2.04 carry the gap; keyword/price/negation near ceiling.

Rules of engagement: every chunk is gated by the 50-query eval harness (judge cache ≈ pennies/run); model spikes are capped at ≤200 calls (user cost rule); corpus-scale spends need explicit user approval with projected cost stated.

| ID | Chunk | Scope | Grounding (research) | Acceptance criteria | Est. cost |
|---|---|---|---|---|---|
| Q1 | `implied-budget` | NLQ: budget words ("cheap", "affordable", "under budget") → store/category-relative price-percentile filter; vertical-config rule + nlq mapping + unit tests | BASELINE-vs-V2 residuals (all 16% violations = implied budgets); Superlinked NLQ described-params | price-violation rate ≤ 0.05 on price-type queries; no regression on other types (full eval run) | ~$0 + 1 eval run |
| Q2 | `query-cache` | Cache NLQ parses + query embeddings keyed by normalized query (Postgres or KV, TTL); head-query full-result cache invalidated on index; keep degraded fallback semantics | Faire EBR phase-1 (KV cache covered ~75% traffic); our own content-hash cache pattern | cached-query p50 < 300ms measured; cached vs fresh results identical on 20 queries; eval unchanged | $0 |
| Q3 | `rerank-spike` (≤200 calls) | bge-reranker (Workers AI, free-tier) and/or RexRanker on top-50 for 20 golden queries; judge-scored A/B vs current ordering | Superlinked hybrid+rerank; RexBERT release; minimaldraft ran bge-reranker in prod; consensus pipelines all rerank | decision artifact: Δ mean@10 measured; SHIP if ≥ +0.05 overall (or ≥ +0.10 on style/use-case) | ~$0 (Workers AI) |
| Q3b | `rerank-ship` (only if Q3 ships) | `Channels.rerank()` or post-RRF rerank stage in core/search.ts, config-driven, off by default; latency budget ≤ +250ms | Q3 artifact | full eval: overall mean@10 ≥ 2.40; latency p50 ≤ 1.3s uncached | 1 eval run |
| Q4 | `multimodal-spike` (≤200 calls / ≤100 images) | Embed 100 products' images (Marqo-FashionSigLIP on Modal = $0, or Gemini multimodal) alongside text vectors; weighted dual-vector retrieval on style-type golden queries | Pinterest unified-embedding lesson; Nuuly 87% Match@20; Amazon "image adds unrecoverable recall"; Mercari +50% CTR | decision artifact: style-subset Δ; corpus rebuild ONLY with user approval + projected cost | ~$0 on Modal |
| Q5 | `lk-vocab-coverage` | Vertical config: LK cultural occasion mappings (poya→white/modest, Kandyan, Avurudu), Singlish/Sinhala/Tamil synonym list (LLM-generated once, reviewed); catalog + ingest 5–10 ethnic-wear stores (feeds only) | Zalando NER→attribute filters; LupaSearch synonym generation + zero-result mining; COSMO offline intent knowledge; batch3 LK schema | local-type mean@10 ≥ 2.0; new-store enrichment cost projected and APPROVED before running (~2k products ≈ $2–4 on cheap stack) | approval-gated |
| Q6 | `enrich-qa-loop` | Surface low-confidence rows + `uncertain_fields` (CLI list + accept/correct), corrections stored per store as few-shot examples, re-enrich corrected rows only | Pixyle correction loop; Walmart confidence-gated QC; Instacart human-in-the-loop; known midi-skirt-as-dress case | demonstrated correction flow on ≥10 real rows; affected golden queries re-judged; no full re-enrichment | <$1 |
| Q8 | `lite-extract-ab` (≤20 calls) | Take 3 products with existing gemini-3-flash-preview stage-2 extractions; re-extract with gemini-3.1-flash-lite (same prompt), diff attribute-by-attribute; iterate prompt tuning (more explicit enum guidance, few-shot) up to 2 rounds; verdict on lite-tier viability per attribute class | User request; Instacart cheap-model finding (simple attrs 95% at −70% cost); cheap-models research (3.1-flash-lite = $0.25/$1.50 vs 3-flash $0.50/$3.00, and 2.5 deprecated) | comparison artifact: per-attribute agreement table across 3 products × 2+ prompt variants; recommendation (which attrs safe on lite, which need 3-flash) | <$0.10 |
| Q7 | `quality-report` | Full 50-query benchmark after shipped chunks; update PARITY.md → QUALITY.md with before/after per lever | F9 discipline (Zalando regression evals) | report with per-chunk attribution; overall ≥ 2.45 mean@10 stretch target | 1 eval run |

Sequencing: Q1 → Q2 (independent, both ~free) → Q3 spike → Q3b if shipped → Q4 spike ∥ Q5 (approval) → Q6 → Q8 → Q7. Total non-approval-gated cost: ≈ a few dollars, dominated by eval runs.

Out of scope (next wave, needs traffic): engagement-trained ranking (Etsy/Walmart), semantic IDs (Eugene Yan), trend-keyword mining from live query logs (Pixyle layer-2 refresh), cross-store dedup wiring (samesake identity config).
