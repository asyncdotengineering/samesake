---
"samesake": minor
---

The bare `samesake` npm name is now a CLI alias for `@samesake/cli`, so `bunx samesake init`
works. Versions ≤ 0.2.0 of this name (an early entity-resolution DSL) are superseded.
